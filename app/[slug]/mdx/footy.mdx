import Image from "next/image"

<h4>old trafford</h4>

<Image
  src="/notes/footy/footy.jpeg"
  alt="Footy"
  width={1200}
  height={800}
  loading="eager"
  className="w-full"
  sizes="(max-width: 768px) 100vw, (max-width: 1280px) 100vw, 1600px"
  quality={85}
/>

<div>
  <h4>fifaaaa</h4>
  <video
    autoPlay
    loop
    muted
    playsInline
    preload="none"
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/fifa1.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <h4>only if he scored that...</h4>
  <video
    autoPlay
    loop
    muted
    playsInline
    preload="none"
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/fifa2.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <h4>bet you did not see that</h4>
  <video
    autoPlay
    loop
    muted
    playsInline
    preload="none"
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/fifa3.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <h4>my fav woman player: alexia putellas</h4>
  <video
    autoPlay
    loop
    muted
    playsInline
    preload="none"
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/alexis.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <h4>defensive work?</h4>
  <video
    autoPlay
    loop
    muted
    playsInline
    preload="none"
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/assasins.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <h4>rare left foot goal?</h4>
  <video
    autoPlay
    loop
    muted
    playsInline
    preload="none"
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/left-footed.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <h4>lucky goal...</h4>
  <video
    autoPlay
    loop
    muted
    playsInline
    preload="none"
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/barca-rejects.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>
